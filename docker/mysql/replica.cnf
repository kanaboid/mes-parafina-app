# Konfiguracja MySQL REPLICA (terminal1).
# read_only ustawiane przez skrypt PO skonfigurowaniu replikacji (nie tutaj —
# inaczej pusta baza nie może się zainicjalizować przy pierwszym starcie).

[mysqld]
server-id=2
relay-log=relay-bin
log-bin=mysql-bin
binlog_format=ROW
