# requirements.in
# =====================================================
# DEPRECATED - Ten plik nie jest już używany
# =====================================================
# Projekt został przeniesiony na zarządzanie przez uv.
# Wszystkie zależności są teraz w: pyproject.toml
# Lockfile znajduje się w: uv.lock
# 
# Ten plik jest zachowany tylko dla kompatybilności.
# Aby dodać nowe zależności użyj: uv add <package>
# =====================================================

# Main dependencies
flask
flask-sqlalchemy
mysql-connector-python
alembic
flask-apscheduler
python-dotenv
networkx
tabulate
flask-socketio
eventlet
pytest
pytz
Flask-Admin
celery
redis
memory-profiler
flower
pyyaml
flask-cors
# Linux
gunicorn
# Windows
pywin32==310; sys_platform == 'win32'