{
    "$schema": "https://railway.com/railway.schema.json",
    "deploy": 
        {
            
            "startCommand": "gunicorn --worker-class eventlet --bind 0.0.0.0:5000 -w 1 --timeout 120 --graceful-timeout 120 --preload run:app"
        }
    
}
